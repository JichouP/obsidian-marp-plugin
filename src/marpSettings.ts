export interface ObsidianMarpPluginSettings {
  createNewSplitTab: boolean;
  autoReload: boolean;
}

export const DEFAULT_SETTINGS: ObsidianMarpPluginSettings = {
  createNewSplitTab: true,
  autoReload: true,
};
